# Base URL of the backend API.
# Leave unset to use the same-origin "/api" prefix that nginx serves in
# production. For local development against a backend on port 8000:
VITE_API_URL="http://localhost:8000/api"
